import 'package:flutter/material.dart';

class OfflineHandling extends StatelessWidget {
  const OfflineHandling({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
